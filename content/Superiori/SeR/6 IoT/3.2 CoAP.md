### CoAP

**CoAP** (*Constrained Application Protocol*) è anch'esso un protocollo client/server ma è:

- (A differenza di HTTP) pensato per ambienti ***constrained*** (vincolati da poche risorse),
- (A differenza di MQTT) indicato per ambienti con **elevato tasso di errore**.

##### Caratteristiche

- Di tipo **REST** (*REpresentational State Transfer*),
- Usa il paradigma client/server di ***request/response*** (client inviano richieste GET/POST ai server per accedere a risorse identificate da URL invece che con i *topic* come MQTT),
- Usa **UDP** a livello <u>trasporto</u>, per cui:
	- (Per UDP) è ***connectionless* e asincrono**,
	- (Per UDP) supporta <u>sia</u> comunicazione ***unicast*** <u>che</u> ***multicast*** (generalmente 1\:1),
	- (Per UDP) <u>non può usare SSL/TLS</u> per la **sicurezza** (di TCP) ma può usare **DTLS** (*Datagram TLS* di UDP).
	- (Nonostante UDP) a livello applicativo può offrire un servizio sia **affidabile** che uno **inaffidabile**,
- I <u>messaggi</u> sono <u>compatibili</u> con **HTTP** (quindi facilmente "traducibili").
- Ha un ***overhead* basso** (*header* in binario di 4 byte)
- I dati trasmessi sono descritti da **metadati** che ne permettono l'interpretazione

##### Esempio

**CoAP** è progettato per interagire con <u>servizi web</u> e con <u>HTTP</u> tramite dei ***proxy*** che <u>traducono i messaggi CoAP in HTTP e viceversa</u>. Per esempio:

- Si usa **CoAP** per comunicazioni <u>da dispositivi IoT a gateway/proxy IoT</u> (**LAN**).
- Si usa **HTTP** per comunicazioni <u>da gateway/proxy IoT a web server in cloud</u> (**WAN**).

![](https://i.imgur.com/znbQVVJ.png)

##### Quando usare CoAP?

###### Esempio

Per esempio in un sistema domotico, dove vari dispositivi IoT devono comunicare con un gateway/server centrale. Il sistema necessita di bassa latenza, trasferimento veloce, basso consumo energetico e bassa velocità.

###### Quando?

- **Quando sono necessari bassa latenza e trasferimento veloce**: CoAP è progettato per app a bassa latenza e che richiedono trasferimenti veloci (per la sua leggerezza e le caratteristiche di UDP (*connectionless*...)).
- **Quando è necessario un basso consumo energetico**: molti dispositivi domotici sono a batteria, ma anche qui la leggerezza di CoAP e la comunicazione in UDP minimizzano il consumo energetico.
- **Quando si ha bassa velocità di trasferimento**: le app domotiche di solito trasmettono piccole quantità di dati (tipo letture di sensori). Per gli stessi motivi di prima CoAP ottimizza la larghezza di banda della rete.
